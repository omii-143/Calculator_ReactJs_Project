name: First Action
on: [push]

jobs:
    first-job:
    runs-on: aquasec/trivy-scan@v2

    steps:
        name: Checkout code
        uses: actions/checkout@v3

        name: Trivy Scan
        uses: aquasec/trivy-scan@v2
        
        run: |
          - trivy fs .
